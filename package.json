{
  "name": "mogu2",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "changeset": "changeset",
    "version-packages": "changeset version && node scripts/sync-cargo-version.mjs",
    "publish-main": "node scripts/publish-main.mjs",
    "echo-noop": "echo 'No-op for changesets action'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0",
    "@huggingface/hub": "^2.13.2",
    "@types/bun": "latest",
    "typescript": "^7.0.1-rc"
  }
}
