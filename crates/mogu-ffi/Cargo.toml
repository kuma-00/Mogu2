[package]
name = "mogu-ffi"
version = "1.0.3" # x-release-please-version
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mogu-core = { path = "../mogu-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libc = "0.2"
image = "0.25.10"
